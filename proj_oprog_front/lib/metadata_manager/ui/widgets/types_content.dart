import 'package:flutter/material.dart';

class TypesContent extends StatelessWidget {
  const TypesContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Types Content', style: TextStyle(fontSize: 24)),
    );
  }
}

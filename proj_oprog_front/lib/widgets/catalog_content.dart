import 'package:flutter/material.dart';

class CatalogContent extends StatelessWidget {
  const CatalogContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Catalog Content', style: TextStyle(fontSize: 24)),
    );
  }
}

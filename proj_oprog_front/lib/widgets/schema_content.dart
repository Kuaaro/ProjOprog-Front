import 'package:flutter/material.dart';

class SchemaContent extends StatelessWidget {
  const SchemaContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Schema Content', style: TextStyle(fontSize: 24)),
    );
  }
}
